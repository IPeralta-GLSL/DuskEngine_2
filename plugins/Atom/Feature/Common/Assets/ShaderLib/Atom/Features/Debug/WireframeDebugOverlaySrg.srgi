#pragma once

ShaderResourceGroup PassSrg : SRG_PerPass
{
    Texture2D<float> m_linearDepthTexture;
    Sampler PointSampler
    {
        MinFilter = Point;
        MagFilter = Point;
        MipFilter = Point;
        AddressU = Clamp;
        AddressV = Clamp;
        AddressW = Clamp;
    };
}
